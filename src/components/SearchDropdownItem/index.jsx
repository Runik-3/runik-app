import PlusCircle from '../PlusCircle/PlusCircle';

export default function SearchDropdownItem({ title, url }) {
    return (
        <div className="flex">
            <PlusCircle />
        </div>
    );
}
